#pragma once
#include "hashtable.h"

/*!
 * \brief choice - function for user choice
 * \param answer - user choice
 * \param hashTable - table for changes
 * \return
 */

bool choice(int answer, HashTable *hashTable);
