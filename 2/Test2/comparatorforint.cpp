#include "comparatorforint.h"

bool ComparatorForInt::compareLess(const int &first, const int &second)
{
    return first < second;
}

bool ComparatorForInt::compareEqual(const int &first, const int &second)
{
    return first == second;
}
